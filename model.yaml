versions: "1.0"

requirements: "./requirements.txt"

image: "mmfewshot:1.6.0-cuda10.1-cudnn7-devel"

tasks:
  train:
    entrypoints: "python ./tools/classification/train.py ./configs/config.py"
    args: 
      num_ways:
      num_shots:
      num_queries:
      num_val_episodes:
      num_test_episodes:
      img_size:
      episodes_seed:
      backbone:
      max_epoch:
  test:
    entrypoints: "python ./tools/classification/test.py ./configs/config.py /export/best_accuracy_mean.pth"
      metrics: